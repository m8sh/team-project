package use_cases.StartScreen;

public class CreateSessionInputData {
    // I feel like I need to have a createSessionInputData just because I have a JoinSessionInputData
    // Unfortunately, this needs no data, but if we ever end up needing this class, it's here I guess
}
